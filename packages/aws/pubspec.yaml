name: aws
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  convert: ^2.1.1

  http: ^0.12.0+2

  rsa_pkcs: ^1.1.0

  pointycastle: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/